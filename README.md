# Personal Dashboard MVP

Локальный адаптивный дашборд задач на .NET 8 + HTML/CSS/JS.

## Что уже есть

- `Backlog` и `Сегодня`.
- Быстрое добавление задачи из свободного текста.
- Короткий title + полное описание + раздел задачи.
- Разделы задач в `Backlog` и `Сегодня`.
- Перетаскивание задач внутри раздела, между разделами и самих разделов за кнопку `☰`; порядок сохраняется в JSON.
- Клик по заголовку раздела сворачивает/разворачивает его.
- Глобальные кнопки «Свернуть всё» / «Развернуть всё» рядом с переключателем `Backlog / Сегодня`.
- Вложенные задачи визуально сдвинуты вправо.
- Очень лёгкий сине-серый оттенок строк разделов.
- Отдельная страница задачи с названием раздела.
- Перенос `Backlog → Сегодня` и `Сегодня → Backlog`.
- В списке перенос обозначен только стрелками `→ / ←`.
- На странице задачи: `→ Сегодня / ← Backlog`.
- Статусы только в `Сегодня`: `Новая → В работе → Завершена → Стикер (удаление)`.
- Цветовые точки: новая — нейтральная, в работе — синяя, завершена — зелёная.
- Фильтры: Все / Новые / В работе / Завершены.
- Адаптивная верстка: sidebar на desktop, bottom navigation на mobile.
- Все изменения сохраняются в одном внешнем `tasks.json`; Docker-клон не владеет данными.
- «База знаний» подключена как отдельный модуль `KnowledgeBase.Api` к единому Dashboard; её дерево и Markdown-документы сохраняются во внешнем `knowledge.json` рядом с `tasks.json`.

## Запуск

Нужен .NET 8 SDK.

```bash
dotnet run
```

После запуска открой адрес, который выведет `dotnet run` (обычно `http://localhost:5xxx`).

## Запуск в Docker (доступ через Tailscale из любого места)

```bash
cp .env.example .env      # при необходимости: путь данных, URL Ollama, ключ OpenRouter
docker compose up -d --build
```

- Приложение слушает `:8080`, а Docker монтирует каталог из `DASHBOARD_DATA_DIR` (по умолчанию в примере: `C:/Main/crystal_wave/Data/personal-dashboard`). Поэтому перенос или повторный клон репозитория не меняет источник задач.
- В каталоге данных лежат: `tasks.json` — текущие задачи, `tasks.json.bak` — состояние до последней записи, `archive/ГГГГ-ММ-ДД/tasks.json` — снимок текущего дня. Хранятся 30 дневных папок; более старые удаляются только после успешной записи нового состояния.
- Если production-файл задач пропал, приложение восстанавливает `tasks.json.bak`; если нет и его, оно явно сообщает об ошибке вместо запуска с пустым списком.
- Контейнер видит Ollama хоста через `host.docker.internal:11434` (добавлен `host-gateway`).
- Общая память агентов берёт ключ из `C:\Main\crystal_wave\Secrets\agent-memory-api-key`. Docker передаёт его в контейнер только для чтения как `/run/secrets/agent-memory-api-key`. На другой машине путь задаётся через `CRYSTAL_WAVE_SECRETS_DIR`.
- OpenRouter API key хранится вне репозитория в `C:\Main\crystal_wave\Secrets\open-router-api-key-vs-code.txt` и монтируется Docker Compose только для чтения как `/run/secrets/openrouter-api-key`; приложение читает его через `OPENROUTER_API_KEY_FILE`. На другой машине каталог задаётся через `CRYSTAL_WAVE_SECRETS_DIR`. Не помещай ключ в `.env`, исходный код или Git.
- Для базы знаний можно задать `KNOWLEDGE_FILE` и `KNOWLEDGE_ALLOWED_ORIGINS`; по умолчанию API использует соседний с `TASKS_FILE` файл, создаёт `.bak` и дневные снимки в `archive/ГГГГ-ММ-ДД/knowledge.json` с хранением 30 дней.
- Для доступа из любой точки: `tailscale serve --bg 8080` (или просто открыть порт в тайлнете) — приложение доступно только устройствам твоей Tailscale-сети, не публичному интернету.

## Структура

- `Program.cs` — API, JSON-хранилище и LLM-агент с каскадом провайдеров.
- Внешний каталог из `DASHBOARD_DATA_DIR` — данные задач и резервные копии; он не должен находиться внутри клона репозитория.
- `wwwroot/index.html` — интерфейс.
- `wwwroot/styles.css` — адаптивные стили.
- `wwwroot/app.js` — UI-логика и работа с API.
- `KnowledgeBase.Api/` — отдельный проект-модуль дерева базы знаний (разделы и Markdown-документы), маршруты которого обслуживает единый Dashboard на `8080`.
- `tests/` — интеграционные тесты (xunit).

## Агент для title/description/section

Сырой надиктованный текст разбирается каскадом провайдеров (`LlmTaskAgent`), интерфейс `ITaskAgent` получает список существующих разделов:

1. **Подписочный DeepSeek через OpenRouter** — основной разборщик, включается автоматически, если задан `OPENROUTER_API_KEY` (модель — `OPENROUTER_MODEL`, по умолчанию `deepseek/deepseek-v4-flash-0731`).
2. **Локальная Ollama** — fallback, `qwen3:4b-instruct-2507-q4_K_M` (по умолчанию `http://localhost:11434`, переопределяется через `OLLAMA_URL` / `OLLAMA_MODEL`). Запрос идёт со строгим JSON-грамматикой (`response_format: json_schema`), модель физически не может выдать кривой JSON.
3. **Эвристика без сети** — title из первого предложения, раздел — по ключевым словам и явному упоминанию существующего раздела. Срабатывает, когда оба LLM лежат.

Приложение всегда отвечает, даже если все LLM лежат.

Модель получает в системном промпте список существующих разделов и должна выбрать самый подходящий (или предложить новое короткое название).

Настройки окружения:
- `TASKS_FILE` — путь к JSON с задачами (по умолчанию `tasks.json` в корне приложения; в Docker — `/data/tasks.json`).
- `TASKS_PROTECTION_ENABLED=true` — включает восстановление из `.bak` и ежедневные архивы; задано в Docker Compose.
- `DASHBOARD_DATA_DIR` — абсолютный каталог хоста, который Docker монтирует как `/data`.

## Тесты

Интеграционные тесты на xunit: создание, перенос между бакетами, жизненный цикл статусов, удаление, персистентность в JSON-файл, каскад провайдеров агента.

```bash
cd tests/PersonalDashboard.Tests && dotnet test
```
